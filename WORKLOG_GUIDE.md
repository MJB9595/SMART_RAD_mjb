# 작업 로그 관리 가이드 (WORKLOG GUIDE)

이 저장소의 작업 기록은 두 폴더로 나눠 관리한다. **다음 에이전트/팀원은 이 문서를 먼저 읽고 규칙에 맞게 작성한다.**

## 폴더 구조
- **`notionmd/`** — *날짜별* 노션 업로드용 종합 로그. 파일명 `YYYY-MM-DD.md`.
  - 그 날 **팀 전체**가 한 일을 담당자별로 구획하여 체계적으로 종합 작성한다.
- **`workMd/`** — *개발자별/기능별* 상세 작업 스펙 및 기술 로그. 파일명 `YYYY-MM-DD_<GitHub핸들>.md`.
  - 예: `2026-07-21_hyunminboo.md`(부현민), `2026-07-22_MJB9595.md`(김재아).

## 작성자(GitHub) ↔ 실명 매핑  ※ 임의 추측 금지, 이 표 기준
| GitHub 핸들 | 실명 | 이메일 |
|---|---|---|
| `bingak36` | 송현빈 (팀장) | qlsl9042@gmail.com |
| `MJB9595` | 김재아 | wansja7870@gmail.com |
| `hyunminboo` | 부현민 | boohyunmin@gmail.com |
| `kim-sungjun592` | 김성준 | kimrlatjdwns@kbu.ac.kr |

## 새 작업 md 만드는 절차 (workMd 템플릿)

모든 개별 작업 로그(`workMd`)는 아래의 표준 양식을 엄격하게 준수하여 작성한다.
Git 커밋 내역(해시, 브랜치)을 분석하여 기술적 변경사항을 구체적으로 남겨야 한다.

```markdown
---
작성자: 실명 (GitHub핸들)
날짜: YYYY-MM-DD
---
# [작업 이력] {작업명}

> 담당: 실명 (GitHub핸들)
> 작업일: YYYY-MM-DD
> 적용 브랜치: {해당 작업을 반영한 주요 브랜치명}
> 적용 커밋: {주요 커밋 해시} ({커밋 메시지 요약})
> 현재 상태: {개발 완료 여부 / 테스트 완료 등}

---

## 1. 작업 목적
(해당 기능을 개발/수정한 이유와 기대 효과를 작성합니다)

## 2. 세부 내역 (API 명세, UI 변경점 등)
(구현된 API의 엔드포인트 테이블, Request/Response 구조, UI 화면 레이아웃 개편 사항 등을 구체적으로 작성합니다)

## 3. 처리 기준 / 비즈니스 로직
(핵심 비즈니스 로직, 데이터 정렬 기준, 예외 처리 등을 명시합니다)

## 4. 공통 기능 및 보안 (선택 사항)
(공통 모듈 추가 내역, 관리자 권한 인증 등 보안적 특이사항을 기술합니다)

## 5. 주요 변경 파일
(표 형태로 주요하게 변경/생성된 파일을 요약합니다)
| 구분 | 파일 | 역할 |

## 6. 테스트 및 검증
(작성된 자동 테스트 내역, 시각적 검증 내역, 발생한 충돌 해결 과정 등을 기술합니다)

## 7. 기타/후속 연동 지점
(프론트엔드 연동 방식 등 다음 작업자를 위한 가이드를 작성합니다)
```

## notionmd 템플릿 가이드

`notionmd`는 `workMd`의 내용을 종합하여 요약하되, 아래 형식을 따른다.
- `## 🧑‍💻 담당자: 실명 (GitHub핸들)` 로 섹션을 나눈다.
- 각 담당자의 작업 단위별로 `### 1. {기능명}` 을 만들고, 아래에 `- **내용**:` 과 `- **결과**:` 형태의 불릿 리스트로 핵심만 명확하게 서술한다.

## 규칙
- 커밋이 **없는 날은 기록하지 않는다**.
- 담당자 표기는 반드시 위 매핑표의 실명을 사용한다.
- `workMd` 작성 시 단순히 '무엇을 했다'가 아니라 깃 커밋 정보를 추적 가능하게 남기고 API/구현 스펙을 최대한 문서화한다.
